version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: p-streamrec-store-p-streamrec_web_1
      APP_PORT: 8080

  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    restart: on-failure
    environment:
      - LOG_LEVEL=info
      - TZ=${TZ:-UTC}
    # Wait for the HTTP server to actually accept connections before
    # p-streamrec starts. Without this, p-streamrec races FlareSolverr's
    # startup and logs "not available" even though it recovers moments later.
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8191/health"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 15s

  web:
    image: ghcr.io/raccommode/p-streamrec:latest
    restart: on-failure
    stop_grace_period: 1m
    depends_on:
      flaresolverr:
        condition: service_healthy
    environment:
      - OUTPUT_DIR=/data
      - PORT=8080
      - FFMPEG_PATH=ffmpeg
      - HLS_TIME=4
      - HLS_LIST_SIZE=6
      - CB_RESOLVER_ENABLED=true
      - AUTO_RECORD_USERS=
      # Optional: set PASSWORD here if you want an extra layer on top of Umbrel auth
      - PASSWORD=
      - TZ=${TZ:-UTC}
      # Optional Chaturbate login (enables Following page + better stream quality)
      - CHATURBATE_USERNAME=
      - CHATURBATE_PASSWORD=
      # FlareSolverr runs as a sibling container inside this app.
      # Docker Compose exposes each service under its name as a DNS alias on
      # the project's internal network, so `flaresolverr` resolves regardless
      # of Umbrel's container naming scheme (_1 vs -1).
      - FLARESOLVERR_URL=http://flaresolverr:8191
    volumes:
      - ${APP_DATA_DIR}/data:/data
